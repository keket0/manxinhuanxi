# Daily Note

日期：2026-04-16

## 今日完成
- 已继续推进 OpenClaw browser 修复，并确认 browser 可正常打开和读取多个网站。
- 已确认 GitHub 网页登录在当前 headless 环境下会触发风控限制，因此后续改走 SSH。
- 已打通 GitHub SSH，确认账号为 `keket0`，并配置了本机 Git 全局身份。
- 已为当前 workspace 接入 GitHub 远程仓库，但随后按主人要求撤回误推送的两个提交。
- 已整理代理分流速查表和超短版，明确 browser、Telegram、GitHub、普通外网命令各自的代理策略。
- 已为按次代理补好本地可复用脚本与快捷入口，并实测多类命令在单独代理下的表现。

## 关键决定
- GitHub 相关推送必须等主人明确发出“推送”指令后才能执行，不能自行 `git push`。
- 以后如果需要代理，只按单次命令或单个工具单独加代理，不恢复全局代理作为默认方案。
- Telegram 专用代理长期保持，不去触碰，后续排查其他代理或网络问题时也不得擅自改动 `channels.telegram.proxy`。

## 失败 / 阻塞
- GitHub 网页登录在当前 headless 环境下被风控拦截，返回 `You can’t perform that action at this time.`，不适合继续死磕网页登录。
- 单独代理并非所有工具表现完全一致：`git`、`pip`、`npm`、部分 `curl/wget` 可用，但 `docker` 外网访问和部分 Python 原生 HTTPS 请求仍不能直接视为已打通。

## 下一步
- 等主人明确要求时，再执行 GitHub 推送。
- 后续遇到外网访问需求时，优先按工具分流，必要时单次命令带代理。
- 如未来再次遇到外网失败，优先先判断具体工具是否需要单独代理，不恢复全局代理；Telegram 线保持原样不动。

- 14:22 主人要求把“全局代理 vs 单独代理”的区别写入长期记忆。已确认长期策略：全局代理影响面大、容易互相打架，并且会伤到 OpenClaw browser；当前机器后续默认保持单独代理，不回到全局代理。
- 14:31 主人要求把当前实际使用的代理地址加入记忆：`http://192.168.50.2:5898`。该地址目前用于 Telegram 专用代理，也用于我按次给单个命令或单个工具临时加代理。
- 14:33 主人进一步明确：Telegram 专用代理要长期保持，不要去触碰。后续即使排查其他代理或网络问题，也不得擅自改动 `channels.telegram.proxy` 这条配置。
- 16:01 已完成 GitHub CLI `gh` 登录，当前账号为 `keket0`，认证信息已持久保存在 `/root/.config/gh/hosts.yml`。后续优先直接复用该登录态，不再默认重复走网页设备码授权。
- 16:08 已复查 browser 当前状态：控制服务 `127.0.0.1:18791` 与 Chrome CDP `127.0.0.1:18800` 都在监听，`browser status` 返回 `running=true`、`cdpReady=true`、`cdpHttp=true`，并已实际打开 `https://example.com/` 成功取回快照。结论是 browser 当前可用，之前报错更像一次性超时或站点策略拦截，不是整体损坏。
- 16:14-16:15 又进一步实测 `https://www.python.org/` 与 `https://github.com/`，均可正常打开并成功读取页面内容，进一步确认 browser 当前整体可用。
- 16:50 后继续排查 OpenClaw 控制台反代登录问题，最终确认 Lucky 将 `http://192.168.50.100:18789/` 反代为 `https://claw.keket.cn:7788/` 后已经可以正常使用密码登录。过程中定位到真实关键点是 `gateway.controlUi.allowedOrigins` 必须包含 `https://claw.keket.cn:7788`，否则日志会报 `origin not allowed`；该结论已上升为长期记忆。
