# 2026-04-21

## 今日完成
- 补建今日日记文件，避免当天工作无记录。
- 记录了主人凌晨问到的影视接口代号与替换流程说明，便于后续追溯。

## 关键决定
- 今日主线暂无新的系统级决策。
- 继续沿用既有规则：接口代号直接映射固定路径，替换前先定位候选，再等主人指定具体目标。

## 遇到的失败/阻塞
- heartbeat 检查时发现今日 `memory/2026-04-21.md` 尚不存在，已补建。

## 下一步
- 如果主人继续发具体源关键字或文件，再按既定替换流程执行。
- 当天有新的实质操作或决定时，继续补记到本文件。

## 简要过程记录
- 主人询问了“接口代号”，已回传 `pg分享`、`pg自用`、`不夜自用`、`不夜分享`、`不夜影视组`、`不夜影视接口` 对应值。
- 主人询问了“替换流程”，已按长期规则说明：先搜候选、等确认、再精确替换；若涉及不夜专用 JS，则先语法检查、给主人下载、确认后再推送、备份冲突文件、重启并检查 `vod` 容器。
