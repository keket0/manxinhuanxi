# 2026-04-19

## 今日完成
- 按主人要求升级 Docker 项目 `wenfxl_codex`，已在 `/www/manmanai/docker/wenfxl_codex` 执行 `docker-compose pull && docker-compose up -d --remove-orphans`，并确认容器 `wenfxl_codex` 仍正常运行，端口映射保持 `8999 -> 8000`。
- 按主人要求重启 `vod` 容器；先发现它不在标准 Docker 项目目录 `/www/manmanai/docker/vod`，后通过容器标签定位到实际 compose 目录 `/www/manmanai/buye`，并成功执行 `docker-compose restart vod`，容器恢复为正常运行状态。
- 处理不夜源替换任务时，先按长期规则列出了 `不夜分享` / `不夜自用` 里的 bilibili 候选条目，避免误替换。
- 随后根据主人明确提供的结构，把 `不夜分享` 与 `不夜自用` 中对应的 bilibili 影视条目替换为 `bili_pgc_only` 路由写法，目标 API 为 `https://by.keket.cn:7777/video/bili_pgc_only`。
- 按长期规则，虽然主人给出的片段里带有 `token: 1992353`，但实际写回 `不夜分享` / `不夜自用` 时仍统一落成 `token: 123456`。
- 按主人要求创建新 agent `agner` 并接入 Telegram：已创建 `/root/.openclaw/workspace-agner` 与 `/root/.openclaw/agents/agner/agent`，并把主 workspace 当前 skills 同步到 `workspace-agner/skills`。
- 已在 `openclaw.json` 中补齐 `agner` agent、Telegram 账号 `agner` 以及 `telegram accountId=agner` 对应 binding。
- 已确认当前 OpenClaw gateway 运行正常，`agner` 已在 `openclaw agents list --bindings` 中显示为 `telegram agner: configured`。
- 主人随后追问“龙虾备份”是否会备份 `agner` 的所有技能和内容。已核对现有 `backup-openclaw` 脚本，确认旧版本只会固定备份 `workspace` 与 `workspace-xiaojizhe`，不会完整收进 `workspace-agner`。随后已按主人要求升级为通用逻辑：自动备份 `/root/.openclaw` 下所有 `workspace*` 目录，因此后续 `workspace-agner` 与未来新建 agent 的独立 workspace 也会一起进入“龙虾备份”。

## 关键决定
- 对 `不夜影视接口` 相关 key 的定位，不能只查接口镜像 JSON；若用户直接给出 JS 源里的 key（如 `bili_pgc_only`），后续应同时查真实不夜 JS 源目录 `/www/manmanai/buye/vod/routes/影视`。
- 即使主人发来替换片段中包含 `1992353`，只要目标是 `不夜分享` / `不夜自用`，写回 token 仍必须保持 `123456`，这条长期规则继续有效。
- 新建 Telegram agent 时，账号 schema 要直接复用现有可用账号结构：`dmPolicy=open`、`groupPolicy=open`、`allowFrom=["*"]`、`commands={native:true,nativeSkills:true}`，不要再写旧/错字段值。
- “龙虾备份”脚本后续不再只写死 `workspace` 与 `workspace-xiaojizhe`，而是统一自动收录 `/root/.openclaw` 下所有 `workspace*` 目录，避免新建 agent 后漏备份其 skills、规则与记忆内容。

## 遇到的失败/阻塞
- 重启 `vod` 时最初误按标准 Docker 目录 `/www/manmanai/docker/vod` 查找，失败原因是该项目实际位于 `/www/manmanai/buye`。
- 创建 `agner` 初次写配置时，`dmPolicy/groupPolicy/commands` 用了不符合当前 schema 的写法，导致 gateway 校验失败；后续已改正。
- 升级 `wenfxl_codex` 时镜像拉取过程较慢，原先的长任务会话最终被系统中断，但单独核对后确认容器仍处于正常运行状态。

## 下一步
- 若主人要求，可继续实测 `agner` 的 Telegram bot 是否能正常收发消息。
- 若主人后续继续做不夜源替换，优先先定位是接口镜像 JSON 条目还是 `不夜影视组` 真实 JS 源 key，再执行替换。
- 若主人后续继续调整 Docker 项目，仍按固定项目目录约定执行，先找准 compose 目录再操作。

## 阶段小结
- 今天新增的稳定经验主要有三条：一是碰到不夜源 `key` 时不能只查接口镜像 JSON，要同步查真实 JS 源目录；二是老 Docker 项目不一定都已经迁到 `/www/manmanai/docker/<项目名>`，必要时要用容器标签反查真实 compose 目录；三是新建 Telegram agent 时，账号 schema 直接照抄现有可用结构最稳，不要自猜字段值。
- 今日的持续项目推进已经同步进 daily note、PROJECTS.md 与 MEMORY.md，当前没有新的用户中断需要继续推进。
