# 2026-04-20

## 今日完成
- 主人再次确认长期规则：所有现有和未来安装/创建的 agent 都必须具备长期记忆、记忆检索，以及每日任务定期总结和日记能力。
- 补充记录技能策略：缺能力时可优先从官方技能站或 GitHub 高 star 免费技能补充，但前提必须免费。
- 已完成一次跨 agent 巡检，新增 `AGENT_RULES_CHECKLIST.md`，并把统一规则补强到主工作区、小记者、agner 的相关文件与今日日记。
- 已替换 `玩偶聚合.js` 为新版本，并修正 JS 内部显示名字为原值 `🈴 玩偶聚合┃4K`；随后已重启 `vod` 容器并确认恢复运行。

## 关键决定
- 将“长期记忆 + 检索 + 日记 + 免费技能优先”视为统一总规则，适用于所有 agent，而不只限于主会话。
- 以后默认使用 `AGENT_RULES_CHECKLIST.md` 作为新建、安装、巡检 agent 的统一验收清单。
- 替换不夜专用 JS 时，若用户口头说“保留原来的名字”，要优先理解为保留 JS 内部展示名，不要只理解成文件名。

## 遇到的失败/阻塞
- 记忆检索未直接命中现成条目，但已补写进长期记忆与项目索引，后续可稳定检索。
- 巡检中发现 `workspace-agner` 当前不是 git 仓库，因此本次只能完成文件级固化，不能像其他工作区一样提交版本记录。
- 重启 `vod` 的第一次状态校验命令写得过死，导致动作已执行但校验误判失败；后续补查容器列表确认 `vod` 实际已正常起来。

## 下一步
- 后续新建或安装 agent 时，默认检查是否继承这套记忆、检索、日记与免费技能规则。
- 已开始巡检并固化到跨 agent 检查清单，后续按清单持续验收。
- 如主人需要，再把 `workspace-agner` 补成 git 仓库，便于后续追踪变更。
- 后续若再替换不夜专用 JS，可按既定流程继续补发下载件、推送影视组、重启并验活。
