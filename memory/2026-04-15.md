- 用户长期偏好已明确并持续适用：中文回复，结论优先，简洁，失败透明，高风险操作先确认。
- 已完成 OpenClaw gateway 全局代理配置，systemd 用户服务环境变量生效：HTTP(S)_PROXY/ALL_PROXY 指向 http://192.168.50.2:5898，NO_PROXY 含 localhost/127.0.0.1/::1/192.168.0.0/16/192.168.50.100；保留 channels.telegram.proxy 不变。
- 已确认 gateway 重启后正常运行，RPC probe ok，监听 127.0.0.1:18789。
- Telegram 菜单问题曾定位到 setMyCommands/deleteWebhook/deleteMyCommands 网络失败；后续用户反馈“菜单功能已经好了”，当前现网状态为 Telegram 在线且菜单正常，暂不继续做高风险变更。
- Telegram 故障排查阶段的关键判断：channels.telegram.proxy 理论上已传入 Telegram transport，问题更像 explicit proxy 模式下 undici + ProxyAgent / transport 兼容层，而不是 commands 配置未开启；已确认存在可调配置 channels.telegram.network.autoSelectFamily 与 dnsResultOrder，但用户未执行该修正，因为菜单已恢复。
- ClawHub 安装能力当前受明显 429 限流影响，多次重试的大部分技能都报 `ClawHub /api/v1/download failed (429): Rate limit exceeded`；应避免紧密重试。
- `claw-ds-generator` 多轮搜索仍未找到真实 slug，更像名字不对/已下架/不存在，不要再盲重试；若要继续需要用户提供原链接或作者信息。
- 当前本地已确认可用技能：weather（bundled）、duckduckgo-search、openai-tts、telegram-bot-manager。
- `phy-openclaw-telegram-bot` 曾在镜像搜索/详情可见，但 CLI 安装返回 404，推断为索引与下载源不同步或条目已不可下载。
- 已为 openai-tts 处理本地依赖：jq 已安装，skills/openai-tts/scripts/speak.sh 已加执行权限；当前唯一剩余阻塞是尚未配置 OPENAI_API_KEY。
- 用户当前明确想继续两条线：等待限流后继续装技能，以及后续验证 TTS；最近一次成功新增安装的技能是 telegram-bot-manager。
- 后续已继续验证 openai-tts 的实际输出文件，确认 `/tmp/openclaw-tts-test.mp3` 并非音频而是 JSON 报错；根因是当前 OpenAI key 无效，服务端返回 `401 invalid_api_key`。因此可明确判断：openai-tts 的网络/代理/脚本链路已打通，真正阻塞点是官方 OpenAI API key 无效，而不是代理或依赖问题。
- 用户提供了大盘工作目录 `/www/manmanai/openclaw` 供后续大文件使用。已确认该目录位于约 1.8T 的盘上，适合作为大文件、下载、浏览器数据、音频输出、产物和日志的落点。
- 已在 `/www/manmanai/openclaw` 下准备好大文件目录结构：`downloads`、`cache`、`artifacts`、`tmp`、`skills-backup`、`browser-data`、`tts-output`、`logs`。默认策略已与用户确认：主目录 `/root/.openclaw` 与 `/root/.openclaw/workspace` 保持原位，大文件优先写到 `/www/manmanai/openclaw`。
- 已发现 `/www/manmanai/openclaw/openclaw.json` 预先存在，但本轮未触碰，保持安全。
- 用户关心“以后重装 OpenClaw/龙虾数据是否还在”。已明确给出持久化策略：大文件目录在 `/www/manmanai/openclaw`，只要 `/www` 盘不动通常仍在；但核心配置与 workspace 仍在系统盘，若重装系统可能丢失，因此建议另做关键目录备份到大盘。
- 已在 `/www/manmanai/openclaw/backup` 下准备备份目录结构：`config`、`workspace`、`memory`、`agents`、`snapshots`，用于后续备份 `~/.openclaw/openclaw.json`、`/root/.openclaw/workspace`、记忆目录和 agents 状态。当前仅建好了落点，尚未开始实际首备份。
- 用户长期偏好已明确并持续适用：中文回复，结论优先，简洁，失败透明，高风险操作先确认。
- 已完成 OpenClaw gateway 全局代理配置，systemd 用户服务环境变量生效：HTTP(S)_PROXY/ALL_PROXY 指向 http://192.168.50.2:5898，NO_PROXY 含 localhost/127.0.0.1/::1/192.168.0.0/16/192.168.50.100；保留 channels.telegram.proxy 不变。
- 已确认 gateway 重启后正常运行，RPC probe ok，监听 127.0.0.1:18789。
- Telegram 菜单问题曾定位到 setMyCommands/deleteWebhook/deleteMyCommands 网络失败；后续用户反馈“菜单功能已经好了”，当前现网状态为 Telegram 在线且菜单正常，暂不继续做高风险变更。
- Telegram 故障排查阶段的关键判断：channels.telegram.proxy 理论上已传入 Telegram transport，问题更像 explicit proxy 模式下 undici + ProxyAgent / transport 兼容层，而不是 commands 配置未开启；已确认存在可调配置 channels.telegram.network.autoSelectFamily 与 dnsResultOrder，但用户未执行该修正，因为菜单已恢复。
- ClawHub 安装能力当前受明显 429 限流影响，多次重试的大部分技能都报 `ClawHub /api/v1/download failed (429): Rate limit exceeded`；应避免紧密重试。
- `claw-ds-generator` 多轮搜索仍未找到真实 slug，更像名字不对/已下架/不存在，不要再盲重试；若要继续需要用户提供原链接或作者信息。
- 当前本地已确认可用技能：weather（bundled）、duckduckgo-search、openai-tts、telegram-bot-manager。
- `phy-openclaw-telegram-bot` 曾在镜像搜索/详情可见，但 CLI 安装返回 404，推断为索引与下载源不同步或条目已不可下载。
- 已为 openai-tts 处理本地依赖：jq 已安装，skills/openai-tts/scripts/speak.sh 已加执行权限；当前唯一剩余阻塞是尚未配置 OPENAI_API_KEY。
- 用户当前明确想继续两条线：等待限流后继续装技能，以及后续验证 TTS；最近一次成功新增安装的技能是 telegram-bot-manager。
- 后续已继续验证 openai-tts 的实际输出文件，确认 `/tmp/openclaw-tts-test.mp3` 并非音频而是 JSON 报错；根因是当前 OpenAI key 无效，服务端返回 `401 invalid_api_key`。因此可明确判断：openai-tts 的网络/代理/脚本链路已打通，真正阻塞点是官方 OpenAI API key 无效，而不是代理或依赖问题。
- 用户提供了大盘工作目录 `/www/manmanai/openclaw` 供后续大文件使用。已确认该目录位于约 1.8T 的盘上，适合作为大文件、下载、浏览器数据、音频输出、产物和日志的落点。
- 已在 `/www/manmanai/openclaw` 下准备好大文件目录结构：`downloads`、`cache`、`artifacts`、`tmp`、`skills-backup`、`browser-data`、`tts-output`、`logs`。默认策略已与用户确认：主目录 `/root/.openclaw` 与 `/root/.openclaw/workspace` 保持原位，大文件优先写到 `/www/manmanai/openclaw`。
- 已发现 `/www/manmanai/openclaw/openclaw.json` 预先存在，但本轮未触碰，保持安全。
- 用户关心“以后重装 OpenClaw/龙虾数据是否还在”。已明确给出持久化策略：大文件目录在 `/www/manmanai/openclaw`，只要 `/www` 盘不动通常仍在；但核心配置与 workspace 仍在系统盘，若重装系统可能丢失，因此建议另做关键目录备份到大盘。
- 已在 `/www/manmanai/openclaw/backup` 下准备备份目录结构：`config`、`workspace`、`memory`、`agents`、`snapshots`，用于后续备份 `~/.openclaw/openclaw.json`、`/root/.openclaw/workspace`、记忆目录和 agents 状态。当前仅建好了落点，尚未开始实际首备份。
- 用户要求以后称呼其为“主人”，该称呼偏好持续有效。
- 已安装并验证独立 `clawhub` CLI 可用，版本为 `ClawHub CLI v0.9.0`；此前 `clawhub login` 失败的真实原因是机器上未安装该命令，而不是 token 格式问题。
- 用户曾在聊天中明文发出 ClawHub token，视为敏感凭据；后续不要回显，且更稳妥的做法是建议用户旋转/更换新 token。
- 已使用用户提供的 token 成功完成 `clawhub login --token ... --no-browser` 并通过 `clawhub whoami` 验证登录态，账号为 `@keket0`。
- 已确认登录态 `clawhub install` 与匿名 `openclaw skills install` 在当前环境下效果不同：前者成功绕过了此前长期遭遇的 429 安装瓶颈，后者长期受匿名下载限流影响。
- 通过已登录的 `clawhub` CLI，已成功补齐两条此前未拿下的核心主线技能：`agent-browser-core` 与 `find-skills-for-clawhub`，并已提交 workspace commit `1077a43`。
- 继续使用已登录的 `clawhub` CLI 后，又成功安装一批此前被 429 卡住的技能：`message-routing`、`liang-tavily-extract`、`reflect`、`agent-browser-stagehand`、`openclaw-find-skills`、`telegram-agent-setup`、`telegram`、`chrome-web-automation`、`openclaw-web-automation`；并已提交 workspace commit `a77e64a`。
- 截至当前，之前“没成功”的候选里真正仍失败且更像不存在/下架/slug 不对的只剩两个：`phy-openclaw-telegram-bot` 与 `claw-ds-generator`；它们在已登录官方 `clawhub` CLI 下仍返回 `Skill not found`，可将问题从“限流”升级定性为“官方当前不可安装”。
- 当前技能学习优先级已收敛：高价值新技能主线为 `agent-browser-core`、`find-skills-for-clawhub`、`liang-tavily-extract`；`message-routing` 也有价值，但它会改 `~/.openclaw/openclaw.json` 和 Telegram 模型路由，属于谨慎使用型技能，不应随便启用。
- 已完成整套已装技能的“真实可用性分级”并写入 `/root/.openclaw/workspace/INSTALLED_SKILLS_MAP.md`，后续判断技能状态应以“实测可战斗 / 参考型 / 需外部 runtime / 包不完整 / 官方不可安装”而非仅以“已安装”区分。
- 已实测通过 `telegram-bot-manager` 自带测试脚本：Telegram API 可达、bot token 有效、`getUpdates` 正常、当前无 webhook，结论是该技能属于真正可用于 Telegram 连通性验证与排障的实战型技能。
- 已实测通过 `liang-tavily-extract`：对 `https://example.com` 的内容抽取返回成功，退出码 0，应将其从“待验证/参考”升级为“已实测可用”。
- `cn-ecommerce-search` 的阻塞已进一步收敛：在补代理后，运行文档声明的依赖 `npx -y @shopmeagent/cn-ecommerce-search-mcp` 返回 npm `404 Not Found`，说明问题不再是单纯网络，而是该 npm 包当前在公开 registry 上不可获取，更像包名失效、下架、私有化或文档过时。
- 已补查 `@shopmeagent` scope 的公开 npm 包，当前只明确看到 `@shopmeagent/logistics-tracking-mcp`，未发现明显可替代 `@shopmeagent/cn-ecommerce-search-mcp` 的同作者电商搜索包，因此不应再按原包名盲撞。
- 已继续反查到技能站存在替代候选 `cn-ecommerce-search-v2`，且 `openclaw skills search cn-ecommerce-search-v2 --json` 可命中；后续若要继续打通中文电商搜索，优先验证 `cn-ecommerce-search-v2`，不要继续围绕旧条目 `cn-ecommerce-search` 的失效 npm 包浪费时间。
- 本轮新增 workspace 提交：`4c6655e docs: add validated skill tiers` 与 `69d7eca docs: update extract and ecommerce validation`。
- 用户在中文电商搜索线的后续选择已进一步明确：在“更容易出结果的平台”方案中，先选了 `1`，要求先把苏宁做成真实商品表；随后又选了 `2`，要求继续追 `iPhone 15` 整机结果并补第二个平台；之后在“继续挖唯品会 / 改成定向详情页或定向入口”之间明确选择了 `B`，即改走“定向详情页 / 定向入口”打法。
- 苏宁线已获得更细的真实性结论：平台本身可战斗，已稳定拿到真实搜索页、真实商品标题、真实详情链接，浏览器快照中可见价格（如 `¥419.00`、`¥309.00`）；但对关键词 `iPhone 15`，搜索结果严重偏向维修/配件而非整机，典型标题包括 `iPhone15pro换外屏...`、`iPhone15外屏碎...`、`iPhone15ProMax换外屏...`。因此苏宁当前问题不是技术链路，而是平台召回质量，适合作为“已打通但结果噪音高”的备用平台。
- 为了改善苏宁结果，已尝试更收敛关键词，包括 `苹果 iPhone 15`、`Apple iPhone 15`、`iPhone 15 手机`、`苹果手机 15`、`iphone15 pro max 苹果`、`苹果15promax`、`苹果 15 plus`、`Apple 15 Plus`、`苹果 15 pro`、`iPhone 15 plus 256G`；结果依旧主要命中换屏、后玻璃、维修、数据线、充电器、钢化膜等噪音项，说明问题在苏宁站内搜索分发，不在关键词本身。
- 苏宁线还验证出一个重要反例：当关键词偏到其他“15 Pro 手机”方向时，苏宁能返回整机类商品（如 `Xiaomi 15 Pro`、`REDMI Note 15 Pro`、`OPPO K15 Pro`、`OPPO Reno15 Pro`），进一步证明苏宁并非抽不出整机，而是“苹果 iPhone 15”这条词路由被维修/配件结果强覆盖。
- 作为第二平台补测，`dangdang` 已能稳定抽取标题、价格、详情链接，但 `iPhone 15` 相关结果主要是图书/手册与周边，不适合整机搜索；当当应归类为“技术可抽取，但业务结果不适合 `iPhone 15` 整机”的平台。
- 本轮对二平台补测的总体判断已更新：目前尚未找到比苏宁更适合做 `iPhone 15` 整机结果的平台。已观测到的第二平台状态包括：`dangdang` 主要是图书/壳类，`vip` 页面能打开但尚未稳定抽出整机商品卡，`gome` / `yihaodian` / `kaola` 直接卡在 SSL/TLS 级别，不是简单 selector 或抽取规则问题。
- 在用户选择 `B` 后，电商线已切换为新的核心策略：不再依赖平台首页泛搜索，而是优先用外部搜索/定向入口定位具体商品详情页 URL，再用 `agent-browser` 对详情页做浏览器态验证。这一策略已被证明优于直接打站内搜索页。
- 定向入口打法已打出关键突破：已确认京东详情页路线可用。通过外部定向搜索拿到了多个真实京东详情页候选，例如 `https://item.jd.com/100066896356.html`、`https://item.jd.com/100067261918.html`、`https://item.jd.com/100068388593.html`、`https://item.jd.com/100068388597.html`，覆盖 `iPhone 15`、`iPhone 15 Pro`、`iPhone 15 Pro Max` 等方向。
- 其中 `https://item.jd.com/100066896356.html` 已被 `agent-browser` 成功打开并看到真实商品详情页，页面标题为 `【苹果iPhone15】Apple/苹果 iPhone 15 (A3092) 128GB 黑色 支持移动联通电信5G 双卡双待【行情 报价 价格 评测】-京东`；快照中可见商品主图、店铺 `Apple产品京东自营旗舰店`、`加入购物车`、`立即购买`、价格区域与面包屑 `手机通讯 > 手机 > 手机 > Apple > 苹果iPhone15`。这证明京东“站内搜索页会进风控，但定向商品详情页可打开”。
- 京东详情页路线的技术分层已更新：直接用 `requests` 抓京东详情页 HTML 仍容易跳转到 `https://www.jd.com?from=pc_item_sd`，因此当前更有效的组合是“外部定向找详情页 URL + agent-browser 浏览器态打开验证”。后续若继续产出 `iPhone 15` 整机结果，应以这条路线为主，而不是再回到京东站内搜索页。
- 当前中文电商平台的最新分层应更新为：1) 苏宁、当当属于“技术可战斗但 `iPhone 15` 结果噪音高”；2) 唯品会属于“可访问但尚未证明能稳定给出整机结果”；3) 京东站内搜索页、淘宝动态结果区、闲鱼、1688、拼多多、以及部分 SSL/TLS 阻塞站点仍各有阻塞；4) 京东定向详情页路线是当前首次真正打到“有用整机详情页”的突破口。
- 用户长期偏好已明确并持续适用：中文回复，结论优先，简洁，失败透明，高风险操作先确认。
- 已完成 OpenClaw gateway 全局代理配置，systemd 用户服务环境变量生效：HTTP(S)_PROXY/ALL_PROXY 指向 http://192.168.50.2:5898，NO_PROXY 含 localhost/127.0.0.1/::1/192.168.0.0/16/192.168.50.100；保留 channels.telegram.proxy 不变。
- 已确认 gateway 重启后正常运行，RPC probe ok，监听 127.0.0.1:18789。
- Telegram 菜单问题曾定位到 setMyCommands/deleteWebhook/deleteMyCommands 网络失败；后续用户反馈“菜单功能已经好了”，当前现网状态为 Telegram 在线且菜单正常，暂不继续做高风险变更。
- Telegram 故障排查阶段的关键判断：channels.telegram.proxy 理论上已传入 Telegram transport，问题更像 explicit proxy 模式下 undici + ProxyAgent / transport 兼容层，而不是 commands 配置未开启；已确认存在可调配置 channels.telegram.network.autoSelectFamily 与 dnsResultOrder，但用户未执行该修正，因为菜单已恢复。
- ClawHub 安装能力当前受明显 429 限流影响，多次重试的大部分技能都报 `ClawHub /api/v1/download failed (429): Rate limit exceeded`；应避免紧密重试。
- `claw-ds-generator` 多轮搜索仍未找到真实 slug，更像名字不对/已下架/不存在，不要再盲重试；若要继续需要用户提供原链接或作者信息。
- 当前本地已确认可用技能：weather（bundled）、duckduckgo-search、openai-tts、telegram-bot-manager。
- `phy-openclaw-telegram-bot` 曾在镜像搜索/详情可见，但 CLI 安装返回 404，推断为索引与下载源不同步或条目已不可下载。
- 已为 openai-tts 处理本地依赖：jq 已安装，skills/openai-tts/scripts/speak.sh 已加执行权限；当前唯一剩余阻塞是尚未配置 OPENAI_API_KEY。
- 用户当前明确想继续两条线：等待限流后继续装技能，以及后续验证 TTS；最近一次成功新增安装的技能是 telegram-bot-manager。
- 后续已继续验证 openai-tts 的实际输出文件，确认 `/tmp/openclaw-tts-test.mp3` 并非音频而是 JSON 报错；根因是当前 OpenAI key 无效，服务端返回 `401 invalid_api_key`。因此可明确判断：openai-tts 的网络/代理/脚本链路已打通，真正阻塞点是官方 OpenAI API key 无效，而不是代理或依赖问题。
- 用户提供了大盘工作目录 `/www/manmanai/openclaw` 供后续大文件使用。已确认该目录位于约 1.8T 的盘上，适合作为大文件、下载、浏览器数据、音频输出、产物和日志的落点。
- 已在 `/www/manmanai/openclaw` 下准备好大文件目录结构：`downloads`、`cache`、`artifacts`、`tmp`、`skills-backup`、`browser-data`、`tts-output`、`logs`。默认策略已与用户确认：主目录 `/root/.openclaw` 与 `/root/.openclaw/workspace` 保持原位，大文件优先写到 `/www/manmanai/openclaw`。
- 已发现 `/www/manmanai/openclaw/openclaw.json` 预先存在，但本轮未触碰，保持安全。
- 用户关心“以后重装 OpenClaw/龙虾数据是否还在”。已明确给出持久化策略：大文件目录在 `/www/manmanai/openclaw`，只要 `/www` 盘不动通常仍在；但核心配置与 workspace 仍在系统盘，若重装系统可能丢失，因此建议另做关键目录备份到大盘。
- 已在 `/www/manmanai/openclaw/backup` 下准备备份目录结构：`config`、`workspace`、`memory`、`agents`、`snapshots`，用于后续备份 `~/.openclaw/openclaw.json`、`/root/.openclaw/workspace`、记忆目录和 agents 状态。当前仅建好了落点，尚未开始实际首备份。
- 用户长期偏好已明确并持续适用：中文回复，结论优先，简洁，失败透明，高风险操作先确认。
- 已完成 OpenClaw gateway 全局代理配置，systemd 用户服务环境变量生效：HTTP(S)_PROXY/ALL_PROXY 指向 http://192.168.50.2:5898，NO_PROXY 含 localhost/127.0.0.1/::1/192.168.0.0/16/192.168.50.100；保留 channels.telegram.proxy 不变。
- 已确认 gateway 重启后正常运行，RPC probe ok，监听 127.0.0.1:18789。
- Telegram 菜单问题曾定位到 setMyCommands/deleteWebhook/deleteMyCommands 网络失败；后续用户反馈“菜单功能已经好了”，当前现网状态为 Telegram 在线且菜单正常，暂不继续做高风险变更。
- Telegram 故障排查阶段的关键判断：channels.telegram.proxy 理论上已传入 Telegram transport，问题更像 explicit proxy 模式下 undici + ProxyAgent / transport 兼容层，而不是 commands 配置未开启；已确认存在可调配置 channels.telegram.network.autoSelectFamily 与 dnsResultOrder，但用户未执行该修正，因为菜单已恢复。
- ClawHub 安装能力当前受明显 429 限流影响，多次重试的大部分技能都报 `ClawHub /api/v1/download failed (429): Rate limit exceeded`；应避免紧密重试。
- `claw-ds-generator` 多轮搜索仍未找到真实 slug，更像名字不对/已下架/不存在，不要再盲重试；若要继续需要用户提供原链接或作者信息。
- 当前本地已确认可用技能：weather（bundled）、duckduckgo-search、openai-tts、telegram-bot-manager。
- `phy-openclaw-telegram-bot` 曾在镜像搜索/详情可见，但 CLI 安装返回 404，推断为索引与下载源不同步或条目已不可下载。
- 已为 openai-tts 处理本地依赖：jq 已安装，skills/openai-tts/scripts/speak.sh 已加执行权限；当前唯一剩余阻塞是尚未配置 OPENAI_API_KEY。
- 用户当前明确想继续两条线：等待限流后继续装技能，以及后续验证 TTS；最近一次成功新增安装的技能是 telegram-bot-manager。
- 后续已继续验证 openai-tts 的实际输出文件，确认 `/tmp/openclaw-tts-test.mp3` 并非音频而是 JSON 报错；根因是当前 OpenAI key 无效，服务端返回 `401 invalid_api_key`。因此可明确判断：openai-tts 的网络/代理/脚本链路已打通，真正阻塞点是官方 OpenAI API key 无效，而不是代理或依赖问题。
- 用户提供了大盘工作目录 `/www/manmanai/openclaw` 供后续大文件使用。已确认该目录位于约 1.8T 的盘上，适合作为大文件、下载、浏览器数据、音频输出、产物和日志的落点。
- 已在 `/www/manmanai/openclaw` 下准备好大文件目录结构：`downloads`、`cache`、`artifacts`、`tmp`、`skills-backup`、`browser-data`、`tts-output`、`logs`。默认策略已与用户确认：主目录 `/root/.openclaw` 与 `/root/.openclaw/workspace` 保持原位，大文件优先写到 `/www/manmanai/openclaw`。
- 已发现 `/www/manmanai/openclaw/openclaw.json` 预先存在，但本轮未触碰，保持安全。
- 用户关心“以后重装 OpenClaw/龙虾数据是否还在”。已明确给出持久化策略：大文件目录在 `/www/manmanai/openclaw`，只要 `/www` 盘不动通常仍在；但核心配置与 workspace 仍在系统盘，若重装系统可能丢失，因此建议另做关键目录备份到大盘。
- 已在 `/www/manmanai/openclaw/backup` 下准备备份目录结构：`config`、`workspace`、`memory`、`agents`、`snapshots`，用于后续备份 `~/.openclaw/openclaw.json`、`/root/.openclaw/workspace`、记忆目录和 agents 状态。当前仅建好了落点，尚未开始实际首备份。
- 用户要求以后称呼其为“主人”，该称呼偏好持续有效。
- 已安装并验证独立 `clawhub` CLI 可用，版本为 `ClawHub CLI v0.9.0`；此前 `clawhub login` 失败的真实原因是机器上未安装该命令，而不是 token 格式问题。
- 用户曾在聊天中明文发出 ClawHub token，视为敏感凭据；后续不要回显，且更稳妥的做法是建议用户旋转/更换新 token。
- 已使用用户提供的 token 成功完成 `clawhub login --token ... --no-browser` 并通过 `clawhub whoami` 验证登录态，账号为 `@keket0`。
- 已确认登录态 `clawhub install` 与匿名 `openclaw skills install` 在当前环境下效果不同：前者成功绕过了此前长期遭遇的 429 安装瓶颈，后者长期受匿名下载限流影响。
- 通过已登录的 `clawhub` CLI，已成功补齐两条此前未拿下的核心主线技能：`agent-browser-core` 与 `find-skills-for-clawhub`，并已提交 workspace commit `1077a43`。
- 继续使用已登录的 `clawhub` CLI 后，又成功安装一批此前被 429 卡住的技能：`message-routing`、`liang-tavily-extract`、`reflect`、`agent-browser-stagehand`、`openclaw-find-skills`、`telegram-agent-setup`、`telegram`、`chrome-web-automation`、`openclaw-web-automation`；并已提交 workspace commit `a77e64a`。
- 截至当前，之前“没成功”的候选里真正仍失败且更像不存在/下架/slug 不对的只剩两个：`phy-openclaw-telegram-bot` 与 `claw-ds-generator`；它们在已登录官方 `clawhub` CLI 下仍返回 `Skill not found`，可将问题从“限流”升级定性为“官方当前不可安装”。
- 当前技能学习优先级已收敛：高价值新技能主线为 `agent-browser-core`、`find-skills-for-clawhub`、`liang-tavily-extract`；`message-routing` 也有价值，但它会改 `~/.openclaw/openclaw.json` 和 Telegram 模型路由，属于谨慎使用型技能，不应随便启用。
- 已完成整套已装技能的“真实可用性分级”并写入 `/root/.openclaw/workspace/INSTALLED_SKILLS_MAP.md`，后续判断技能状态应以“实测可战斗 / 参考型 / 需外部 runtime / 包不完整 / 官方不可安装”而非仅以“已安装”区分。
- 已实测通过 `telegram-bot-manager` 自带测试脚本：Telegram API 可达、bot token 有效、`getUpdates` 正常、当前无 webhook，结论是该技能属于真正可用于 Telegram 连通性验证与排障的实战型技能。
- 已实测通过 `liang-tavily-extract`：对 `https://example.com` 的内容抽取返回成功，退出码 0，应将其从“待验证/参考”升级为“已实测可用”。
- `cn-ecommerce-search` 的阻塞已进一步收敛：在补代理后，运行文档声明的依赖 `npx -y @shopmeagent/cn-ecommerce-search-mcp` 返回 npm `404 Not Found`，说明问题不再是单纯网络，而是该 npm 包当前在公开 registry 上不可获取，更像包名失效、下架、私有化或文档过时。
- 已补查 `@shopmeagent` scope 的公开 npm 包，当前只明确看到 `@shopmeagent/logistics-tracking-mcp`，未发现明显可替代 `@shopmeagent/cn-ecommerce-search-mcp` 的同作者电商搜索包，因此不应再按原包名盲撞。
- 已继续反查到技能站存在替代候选 `cn-ecommerce-search-v2`，且 `openclaw skills search cn-ecommerce-search-v2 --json` 可命中；后续若要继续打通中文电商搜索，优先验证 `cn-ecommerce-search-v2`，不要继续围绕旧条目 `cn-ecommerce-search` 的失效 npm 包浪费时间。
- 本轮新增 workspace 提交：`4c6655e docs: add validated skill tiers` 与 `69d7eca docs: update extract and ecommerce validation`。
- 用户在中文电商搜索线的后续选择已进一步明确：在“更容易出结果的平台”方案中，先选了 `1`，要求先把苏宁做成真实商品表；随后又选了 `2`，要求继续追 `iPhone 15` 整机结果并补第二个平台；之后在“继续挖唯品会 / 改成定向详情页或定向入口”之间明确选择了 `B`，即改走“定向详情页 / 定向入口”打法。
- 苏宁线已获得更细的真实性结论：平台本身可战斗，已稳定拿到真实搜索页、真实商品标题、真实详情链接，浏览器快照中可见价格（如 `¥419.00`、`¥309.00`）；但对关键词 `iPhone 15`，搜索结果严重偏向维修/配件而非整机，典型标题包括 `iPhone15pro换外屏...`、`iPhone15外屏碎...`、`iPhone15ProMax换外屏...`。因此苏宁当前问题不是技术链路，而是平台召回质量，适合作为“已打通但结果噪音高”的备用平台。
- 为了改善苏宁结果，已尝试更收敛关键词，包括 `苹果 iPhone 15`、`Apple iPhone 15`、`iPhone 15 手机`、`苹果手机 15`、`iphone15 pro max 苹果`、`苹果15promax`、`苹果 15 plus`、`Apple 15 Plus`、`苹果 15 pro`、`iPhone 15 plus 256G`；结果依旧主要命中换屏、后玻璃、维修、数据线、充电器、钢化膜等噪音项，说明问题在苏宁站内搜索分发，不在关键词本身。
- 苏宁线还验证出一个重要反例：当关键词偏到其他“15 Pro 手机”方向时，苏宁能返回整机类商品（如 `Xiaomi 15 Pro`、`REDMI Note 15 Pro`、`OPPO K15 Pro`、`OPPO Reno15 Pro`），进一步证明苏宁并非抽不出整机，而是“苹果 iPhone 15”这条词路由被维修/配件结果强覆盖。
- 作为第二平台补测，`dangdang` 已能稳定抽取标题、价格、详情链接，但 `iPhone 15` 相关结果主要是图书/手册与周边，不适合整机搜索；当当应归类为“技术可抽取，但业务结果不适合 `iPhone 15` 整机”的平台。
- 本轮对二平台补测的总体判断已更新：目前尚未找到比苏宁更适合做 `iPhone 15` 整机结果的平台。已观测到的第二平台状态包括：`dangdang` 主要是图书/壳类，`vip` 页面能打开但尚未稳定抽出整机商品卡，`gome` / `yihaodian` / `kaola` 直接卡在 SSL/TLS 级别，不是简单 selector 或抽取规则问题。
- 在用户选择 `B` 后，电商线已切换为新的核心策略：不再依赖平台首页泛搜索，而是优先用外部搜索/定向入口定位具体商品详情页 URL，再用 `agent-browser` 对详情页做浏览器态验证。这一策略已被证明优于直接打站内搜索页。
- 定向入口打法已打出关键突破：已确认京东详情页路线可用。通过外部定向搜索拿到了多个真实京东详情页候选，例如 `https://item.jd.com/100066896356.html`、`https://item.jd.com/100067261918.html`、`https://item.jd.com/100068388593.html`、`https://item.jd.com/100068388597.html`，覆盖 `iPhone 15`、`iPhone 15 Pro`、`iPhone 15 Pro Max` 等方向。
- 其中 `https://item.jd.com/100066896356.html` 已被 `agent-browser` 成功打开并看到真实商品详情页，页面标题为 `【苹果iPhone15】Apple/苹果 iPhone 15 (A3092) 128GB 黑色 支持移动联通电信5G 双卡双待【行情 报价 价格 评测】-京东`；快照中可见商品主图、店铺 `Apple产品京东自营旗舰店`、`加入购物车`、`立即购买`、价格区域与面包屑 `手机通讯 > 手机 > 手机 > Apple > 苹果iPhone15`。这证明京东“站内搜索页会进风控，但定向商品详情页可打开”。
- 京东详情页路线的技术分层已更新：直接用 `requests` 抓京东详情页 HTML 仍容易跳转到 `https://www.jd.com?from=pc_item_sd`，因此当前更有效的组合是“外部定向找详情页 URL + agent-browser 浏览器态打开验证”。后续若继续产出 `iPhone 15` 整机结果，应以这条路线为主，而不是再回到京东站内搜索页。
- 当前中文电商平台的最新分层应更新为：1) 苏宁、当当属于“技术可战斗但 `iPhone 15` 结果噪音高”；2) 唯品会属于“可访问但尚未证明能稳定给出整机结果”；3) 京东站内搜索页、淘宝动态结果区、闲鱼、1688、拼多多、以及部分 SSL/TLS 阻塞站点仍各有阻塞；4) 京东定向详情页路线是当前首次真正打到“有用整机详情页”的突破口。
- 新增 Telegram 媒体链路诊断结论：用户反馈“⚠️ Failed to download media. Please try again.”并不是没发成功，而是 OpenClaw 在下载 Telegram 媒体文件时失败。日志曾收敛到精确错误：`MediaFetchError: Failed to fetch media from https://api.telegram.org/file/...` 与 `fetch failed | Client network socket disconnected before secure TLS connection was established`。
- 后续根因被用户现场纠正并验证：**真正原因不是代码/代理链本身，而是 Telegram 机器人隐私模式此前被禁用；用户手动重新打开后，图片已可正常传入会话并被 OpenClaw 成功读取。**
- 因此，之前围绕 Telegram 媒体下载 transport / ProxyAgent / TLS 的静态分析与补丁尝试，应降级为“排障过程中的错误方向或至多是旁支风险”，不能再作为本问题的真实根因结论。
- 当前对本次 Telegram 图片失败的最终最小结论应更新为：**根因是 Telegram bot 隐私模式设置，已由用户手动修复，现已恢复图片接收。**
- 新的稳定事实：本次 Telegram 图片问题的最终根因以用户现场修正为准，不是此前猜测的 transport/Proxy/TLS 代码层问题，而是 **Telegram 机器人隐私模式设置**；用户手动打开后，图片已能正常进入会话并被成功读取。
- 因此，之前针对 Telegram 媒体下载做的源码补丁、transport 推断和 network 参数猜测，都不应再被当作这次故障的根修结论，最多只能算排障过程中的旁支尝试。
- 用户最新明确要求：继续去官网看技能，安装“排名前 5”的技能，并把所有已安装过的技能深入学习。
- 已透明确认一个现实限制：官网/CLI 当前排行榜相关接口返回空列表，无法可靠拿到“官网真实前 5”；因此本轮只能降级为安装当前能确定的热门/相关候选，而不能声称拿到了真实前 5 榜单。
- 本轮新装成功的技能包括：`agent-browser-clawdbot`、`telegram-bot-builder`、`telegram-history`、`openclaw-telegram-setup`、`weather`、`weather-1-0-0`。
- `browser-automation` 本轮未安装，原因不是下载失败，而是被标记为 suspicious；原始限制为需要 `--force` 才能在非交互模式安装。按用户既定安全偏好，这类风险安装不能擅自强装。
- 本轮已对一批已安装技能做了更深入学习和分层判断：
  - 当前第一梯队实战主力：`agent-browser-core`、`agent-browser-clawdbot`、`duckduckgo-search`、`liang-tavily-search`、`telegram-bot-manager`、`weather`。
  - 第二梯队有条件可用：`telegram-history`、`cn-ecommerce-search-v2`、`openai-tts`（换有效 key 后可恢复）。
  - 主要参考型：`telegram-bot-builder`、`openclaw-telegram-setup`、`telegram-agent-setup`、`chrome-web-automation`、`find-skills-for-clawhub`、`openclaw-find-skills`、`proactive-agent-lite`、`self-improvement`、`self-improving-proactive-agent`、`message-routing`、`reflect`。
  - 当前不建议投入：`cn-ecommerce-search`、`agent-browser-stagehand`、`openclaw-web-automation`。
- `weather` 与 `weather-1-0-0` 目前判断为功能重复，主用 `weather` 即可。
- 本轮新增 workspace 提交：`0250acb feat: install additional clawhub skills`。
- 用户在 2026-04-15 新增长期要求：我必须具备长期记忆与记忆检索能力；每天做的任务要定期整理总结和日记；如需补能力，可优先从官方技能站或 GitHub 找免费且高价值的技能安装，但不能为了数量盲装。
- 已据此落地当前工作流：新增 `/root/.openclaw/workspace/SESSION-STATE.md` 作为活跃状态文件；更新 `HEARTBEAT.md`，要求 heartbeat 优先补当天记忆、日记摘要、长期记忆沉淀与免费技能补强。
- 已初始化长期记忆总表 `/root/.openclaw/workspace/MEMORY.md`，用于沉淀稳定长期事实，不再只依赖每日记忆文件。
- 已新增 `/root/.openclaw/workspace/memory/DAILY_TEMPLATE.md`，作为后续每日总结 / 日记的固定模板。
- 已新增 `/root/.openclaw/workspace/PROJECTS.md`，作为持续项目、长期待办、当前状态与下一步的统一索引。
- 已执行当前状态备份，最新快照目录为 `/www/manmanai/openclaw/backup/snapshots/2026-04-15-143902`；本次快照总量约 `46M`，其中 `agents` 约 `42M`，`workspace` 约 `3.8M`，`memory` 约 `72K`，`config` 约 `12K`。

## 今日完成
- 完成 OpenClaw 全局代理配置与验活，保留 Telegram 独立代理配置不变。
- 排查并确认 Telegram 菜单已恢复，图片接收问题最终结案为机器人隐私模式设置所致。
- 完成技能体系一轮安装、验证、分层与文档化，补齐多项实用技能。
- 完成 NVIDIA 备用模型接入、备份体系初始化、当前状态备份与恢复说明文档编写。
- 建立长期记忆、项目索引、日记模板与 heartbeat 推进规则。

## 关键决定
- 持续项目与长期待办统一维护在 `PROJECTS.md`。
- 以后涉及过去事项，先做记忆检索再回答。
- 中文电商搜索主线切换为 `cn-ecommerce-search-v2` + 外部搜索 + `agent-browser` + 定向详情页。
- 对 suspicious 技能不擅自强装，优先免费、可验证、少而精的技能路线。
- 备份策略采用“主目录留原位，大文件与快照落到 `/www/manmanai/openclaw`”。

## 失败 / 阻塞
- `openai-tts` 当前阻塞于无效 OpenAI key，不是网络或代理问题。
- `claw-ds-generator` 仍找不到真实可安装条目，更像下架、未公开或名称有误。
- Telegram 仍存在少量其他 API 偶发失败日志，但已与图片问题拆开看待。
- 内置 `browser` 工具仍受全局代理与 SSRF policy 冲突影响，当前不能作为主力浏览器线。

## 下一步
- 如无新中断，继续维护 daily note 与长期记忆沉淀。
- 如主人继续推进电商线，优先沿京东定向详情页路线整理商品表。
- 如主人继续完善恢复能力，下一步可考虑把关键全局 CLI 清单与更多系统级配置纳入备份。
- 如主人提供有效 OpenAI key，可立即恢复 TTS 实战能力。

- 已按用户要求把 `/root/.config/systemd/user/openclaw-gateway.service` 纳入备份脚本；当前副本位于 `/www/manmanai/openclaw/backup/systemd/openclaw-gateway.service`，最新含该文件的快照为 `/www/manmanai/openclaw/backup/snapshots/2026-04-15-152820`。
- 已同步更新恢复文档 `/root/.openclaw/workspace/RESTORE.md`，当前恢复范围已从 OpenClaw 配置/工作区/记忆/agents 扩展到 gateway 的 systemd 用户服务文件。
- 已新增跨机器迁移文档 `/root/.openclaw/workspace/MIGRATION.md`，明确了新机器先装什么、迁哪些文件、如何重载 systemd、如何验活，以及最容易卡在 Node 路径 / OpenClaw 安装路径 / 代理可达性 / 外部 CLI 缺失这些点。
- 已新增 CLI 清单文档 `/root/.openclaw/workspace/CLI-INVENTORY.md`，把当前机器上的关键命令按“必装 / 强烈建议装 / 辅助增强”分层，并标明缺失后会影响哪条能力线，方便后续跨机器恢复。
- 已新增迁移前置检查脚本 `/root/.openclaw/workspace/scripts/check-migration-prereqs.sh`，会输出 PASS / WARN / FAIL，并检查必装命令、建议命令、systemd 用户环境、service 文件 ExecStart、Telegram 代理配置和 gateway 监听端口；在当前机器实测结果为 `PASS=20 WARN=0 FAIL=0`。
- 已新增半自动迁移脚本 `/root/.openclaw/workspace/scripts/migrate-openclaw-from-backup.sh`，支持从 `current` 或指定/最新 `snapshot` 恢复到目标机，默认 dry-run，`--apply` 才真正写入，`--restart-service` 才执行 systemd 重载与 gateway 重启；脚本会先把目标机原有文件移动到临时目录做现场备份。
- 已新增新机器准备检查脚本 `/root/.openclaw/workspace/scripts/check-new-machine-readiness.sh`，用于在恢复前检查 Linux / root 路径基线 / systemd 用户环境 / node / openclaw / 建议 CLI / 备份目录 / 参考代理可达性；在当前机器实测结果为 `PASS=20 WARN=0 FAIL=0`。
- 已把半自动迁移脚本扩展为支持选择性恢复：可用 `--only config`、`--only memory`、`--only skills` 或组合值（如 `config,memory,service`）；其中 `skills` 只恢复 `workspace/skills`，不会覆盖整个 workspace。
- 用户要求记住一组常用官方入口，后续查 OpenClaw 资料优先使用：`https://openclaw.ai`、`https://docs.openclaw.ai`、`https://github.com/openclaw/openclaw`、`https://clawhub.ai`、`https://github.com/VoltAgent/awesome-openclaw-skills`。
- 用户再次强调：以后都要用中文回答。
- 用户进一步强调：以后回复里不要夹英文；除非是在引用原始报错、字段名、命令名，否则说明文字一律只用中文。
- 已记录影视接口代号映射：`pg分享`=`/www/manmanai/PG/jsm🧿.json`，`pg自用`=`/www/manmanai/pg_zy/jsm🧿.json`，`不夜自用`=`/www/manmanai/pg_zy/buyezy🧿.json`，`不夜分享`=`/www/manmanai/pg_zy/buyegg🧿.json`；后续用户直接报代号即可定位文件。
- 已系统学习 OmniBox 写 T4 源的三份文档，并整理出结论：`type: 4` 本质是 OmniBox 后端接口型源，真正开发重点在 `home/category/detail/search/play` 五个 handler、`context.from` 的端差异、结构化 `vod_play_sources` / `urls` 返回、网盘 SDK、缓存、刮削与播放辅助能力；学习笔记已写入 `/root/.openclaw/workspace/OMNIBOX_T4_DEEP_DIVE_2026-04-15.md`。
- 用户明确了 OmniBox 开发偏好：那 4 个影视 JSON 以后主要只用于代号定位和最终挂载，不用拿来当写源模板；真正要做的是深度学 OmniBox 和目标网站，让用户以后直接给网站就能尽量写成可用的 JS 源。
- 用户发送了一份 OmniBox 的豆瓣推荐 JS 源示例；已将其拆成可复用写法模式，重点吸收首页 `class/list/filters/banner` 组织、`categoryId` 分流、图片代理、字段标准化和错误降级思路，笔记写入 `/root/.openclaw/workspace/OMNIBOX_JS_PATTERN_DOUBAN_2026-04-15.md`。
- 用户进一步明确学习方式：以后收到示例脚本时，要参考“怎么写”和背后的结构思路，跟着学可复用模式，而不是只看结果或机械照抄。
- 已对 `Silent1566/OmniBox-Spider` 仓库做类型化学习，按“采集站 / 网盘 / 解析 / 直播 / 音乐 / 短剧 / Emby / 综合平台 / 推送模板”等维度整理出“网站类型 -> 推荐写法”作战笔记，写入 `/root/.openclaw/workspace/OMNIBOX_SPIDER_TYPE_PLAYBOOK_2026-04-15.md`。
- 主人明确表示以后还会继续给 OmniBox JS 示例，后续默认按“学习写法 -> 提炼模式 -> 沉淀笔记 -> 反哺实战写源”的方式处理。
- 已按主人的新配置把“不夜自用”与“不夜分享”里的 B 站源替换为 `bili_all_mix`，目标文件分别是 `/www/manmanai/pg_zy/buyezy🧿.json` 与 `/www/manmanai/pg_zy/buyegg🧿.json`；新条目使用 `https://by.keket.cn:7777/video/bili_all_mix`，并启用 `quickSearch: 1` 与 `filterable: 1`。
- 主人明确纠正：不夜源写法与 OmniBox 写法不通用，不能混着理解；已据此单独拆解不夜源的挂载体系，并整理出按 `type:3/type:4`、`api` 形态、本地模块 / 本地脚本 / 运行时规则 / 远端接口、以及 Emby / B站 / 盘搜 / 站群复用 / 直播 / 工具源等维度的学习笔记，写入 `/root/.openclaw/workspace/BUYE_SOURCE_PATTERNS_2026-04-15.md`。

## 持久事实摘要 (2026-04-15)

- 用户偏好：中文回复，结论优先，简洁，失败透明，高风险操作先确认，配置/核心技能变更遵循既定安全流程，搜索与外部信息优先既定搜索链路，不确定时明确说明。

- 称呼：用户要求以后称呼其为“主人”。

- Telegram 问题根因：图片接收失败最终由用户确认为 Telegram 机器人隐私模式被禁用，用户手动重新打开后恢复。（之前的 transport/TLS/代理分析被排除。）

- 备份策略：大文件目录 /www/manmanai/openclaw，快照脚本 /root/.openclaw/workspace/scripts/backup-openclaw.sh，最新快照 /www/manmanai/openclaw/backup/snapshots/2026-04-15-143902。

- 恢复指南：见 /root/.openclaw/workspace/RESTORE.md。

- 长期记忆体系：已建立 SESSION-STATE.md、MEMORY.md、memory/YYYY-MM-DD.md、memory/DAILY_TEMPLATE.md、HEARTBEAT.md、PROJECTS.md。

- 项目索引：已在 PROJECTS.md 中列出 Telegram 稳定性、技能体系建设、中文电商搜索、长期记忆与日记、备份与可恢复性、模型与 TTS 能力线，并标注优先级、更新时间、阻塞状态、触发条件。

- 技能体系：官方榜单接口当前返回空，无法可靠获取“真实前 5”。已安装技能按真实可用性分层（主力/有条件可用/参考型/不建议投入）。

- 指令遵循：回答过去工作、决定、偏好、待办时，必须先做记忆检索。
## Heartbeat 精简摘要（15:46）
- 今天后半段的核心推进已从“能备份”扩展到“更可恢复、可迁移”。
- 已把 `/root/.config/systemd/user/openclaw-gateway.service` 正式纳入备份脚本；当前副本位于 `/www/manmanai/openclaw/backup/systemd/openclaw-gateway.service`，最新含该文件的快照为 `/www/manmanai/openclaw/backup/snapshots/2026-04-15-152820`。
- 已补齐三份恢复侧文档：`SYSTEM-STATE.md`、`MIGRATION.md`、`CLI-INVENTORY.md`，分别覆盖当前机器状态、跨机器迁移步骤、关键 CLI 清单。
- 当前恢复/迁移链路的最小资料包已明确：`backup/` + `RESTORE.md` + `SYSTEM-STATE.md` + `MIGRATION.md` + `CLI-INVENTORY.md`。
- 后续最值得继续补的方向，不再是基础备份，而是“半自动迁移前置检查脚本”或“更完整的系统级清单”。

## Heartbeat 精简摘要（16:14）
- 已把恢复链的最新状态回写到 `PROJECTS.md`，避免项目索引仍停留在只有备份和迁移文档、却遗漏三份新脚本的旧状态。
- 当前备份与可恢复性线已形成三段式流程：`check-new-machine-readiness.sh` -> `check-migration-prereqs.sh` -> `migrate-openclaw-from-backup.sh`。
- 当前这条线的下一步已明确收敛为：如需继续增强，优先补 systemd / crontab / 其他系统级自定义项清单，而不是重复扩展已有迁移脚本。

## Heartbeat 精简摘要（16:45）
- 已把“默认全中文回复、非必要不夹英文”从今日日记提升到 `MEMORY.md`，避免后续只记在当天日志里。
- 当前长期稳定的用户偏好已在长期记忆和日记两层都对齐，后续即使跨会话也应保持只用中文说明。

## 今日补充进展（17:44）
- 主人新增并确认四个影视接口代号路径：`pg分享=/www/manmanai/PG/jsm🧿.json`、`pg自用=/www/manmanai/pg_zy/jsm🧿.json`、`不夜自用=/www/manmanai/pg_zy/buyezy🧿.json`、`不夜分享=/www/manmanai/pg_zy/buyegg🧿.json`。
- 已把代号路径写入 `TOOLS.md`、`USER.md` 和 `MEMORY.md`，后续主人只报代号即可直接定位文件，不再重复询问路径。
- 主人再次强调：以后继续发送 JS 示例时，会明确说明这是不夜源写法还是 OmniBox 写法；我必须按标注分类学习，不能搞混。
- 已替换 `不夜自用` 与 `不夜分享` 中的 `🅱️ bilibili ┃ B站` 条目，新配置改为 `key=bilibili`、`api=https://by.keket.cn:7777/video/bilibili`、`quickSearch=0`、`changeable=0`。
- 已分别校验 `不夜自用` 与 `PG分享` 的 JSON 语法，当前都可正常解析。

## 关键决定（追加）
- 四个影视接口以后一律按代号工作，不再靠临时搜索文件名。
- 学习 JS 示例与修改四个 JSON 配置必须继续严格分离，避免把“学习写法”和“挂载配置”混在一起。

## 下一步（追加）
- 后续主人如果按代号要求修改或同步配置，直接定位对应文件执行。
- 继续在收到新 JS 示例时，先按主人标注判断是不夜源还是 OmniBox，再学习和归类。

## 今日补充进展（不夜 routes 深学）
- 已开始系统学习 `/www/manmanai/buye/vod/routes`，并确认这不是单纯的站源目录，而是完整的不夜后端路由体系。
- 当前已按类型拆出 5 条主线：`影视/`、`网盘/`、`配置/`、`其他/`、`API/`。
- 已抽样深入阅读的代表文件包括：`影视/bilibili.js`、`影视/哔哩大全融合版.js`、`影视/立播影视.js`、`网盘/盘链直登盘检.js`、`其他/全球追更.js`、`配置/玩偶聚合.js`、`API/source.js`、`API/parse.js`。
- 已整理出一份独立笔记：`BUYE_ROUTES_DEEP_DIVE_2026-04-15.md`，核心结论是“不夜源更像 Fastify 路由模块开发，不同于 OmniBox 的 handler 框架开发”。
- 已继续深挖 `影视/`、`网盘/`、`配置/`，确认不夜影视源内部至少分为：配置驱动型网页站源、专用平台型、轻量独立脚本型；网盘侧则明显分为网盘驱动型与盘搜聚合型两条路线。
- 已确认不夜盘搜聚合类的稳定流水线是：盘搜接口 → 原始链接拉平去重 → 链接校验 → 排序清洗 → `drive.getVod()` 落播放。
- 已新增整理一份可直接复用的模板笔记：`BUYE_SOURCE_TEMPLATES_2026-04-15.md`，里面拆出了不夜影视源标准模板、不夜盘搜源标准模板、以及聚合源补充模板。
- 主人随后再次要求把 `不夜自用` 与 `不夜分享` 的 `🅱️ bilibili ┃ B站` 改回 `bili_all_mix` 配置；现状态已更新为 `key=bili_all_mix`、`api=https://by.keket.cn:7777/video/bili_all_mix`、`quickSearch=1`、`filterable=1`，并已重新通过两个文件的 JSON 语法校验。
- 已将“影视源学习与配置维护线”正式补入 `PROJECTS.md`，把代号映射、学习边界、不夜/OmniBox 区分规则、已沉淀笔记和后续触发条件统一纳入项目索引，避免后续 heartbeat 只追 OpenClaw 主线却遗漏这条已长期化的影视工作线。
- 主人新增一条后续配置修改规则：当要求修改 `不夜自用`、`不夜分享`、`pg自用`、`pg共享/pg分享` 时，可能只会给“源关键字”。此时我应先在对应代号文件里搜索并列举命中的候选源，再等主人指定具体哪一项后再执行替换，不能直接猜目标条目。

## Heartbeat 精简摘要（19:44）
- 今天后半段新增了一条已长期化的工作主线：影视源学习与配置维护，已经正式进入 `PROJECTS.md`，后续不会再只散落在聊天和当日日志里。
- 这条线当前的稳定工作方式已经定型：按代号直接改四个 JSON，学习 JS 示例时严格区分“不夜源”和“OmniBox/monibox”，写新源前若类型未明确就先确认。
- 不夜侧已沉淀两类可直接复用资料：一类是架构理解与类型拆分笔记，另一类是标准模板笔记，后续进入实战写源时不需要再从零摸索。

## 今日补充进展（21:45）
- 已继续排查 OpenClaw 局域网网页登录失败，确认当前阻塞并非单纯密码错误，而是局域网纯 HTTP 下的 Control UI 设备身份校验限制。
- 已按临时应急思路在 `/root/.openclaw/openclaw.json` 中开启 `gateway.controlUi.dangerouslyDisableDeviceAuth=true`，并保留当前密码登录模式，供主人先在局域网地址上继续使用网页。
- 当前 OpenClaw 网页入口仍为 `http://192.168.50.100:18789/`，认证模式仍是 `password`。
- 已确认这只是破窗式临时方案，后续更稳妥的正路仍是给 OpenClaw Web 补 HTTPS，再撤回 `dangerouslyDisableDeviceAuth`。
- 另已确认当前这版 Web 界面虽然存在语言设置入口，但中文本地化并不完整，所以大量界面文案仍会显示英文，这属于当前版本界面现状，不是主人设置错误。

## 关键决定（追加）
- OpenClaw Web 当前先采用“局域网可用优先”的临时策略，短期保留 `dangerouslyDisableDeviceAuth=true` 以绕过纯 HTTP 下的设备身份校验阻塞。
- 后续如继续优化 OpenClaw Web，优先级最高的是补 HTTPS，然后撤销这项破窗配置，而不是继续长期裸跑局域网 HTTP。

## 失败 / 阻塞（追加）
- OpenClaw Control UI 在局域网纯 HTTP 下会受非安全上下文与设备身份校验限制，导致即使密码正确也可能无法正常登录。
- 当前 Web 中文化不是配置问题，而是这版前端本身中文翻译覆盖不完整。

## 下一步（追加）
- 若主人继续推进 OpenClaw Web，可二选一：继续临时使用当前局域网 HTTP 方案，或切换到 HTTPS 正式方案。
- 后续若改成 HTTPS，应同时撤回 `dangerouslyDisableDeviceAuth`，恢复更正常的网页安全边界。

## 今日补充进展（Docker 管理约定）
- 主人明确了后续 Docker 项目的统一管理方式：所有项目都放在 `/www/manmanai/docker` 下，并按项目名创建独立目录。
- 每个 Docker 项目目录内固定放置 `docker-compose.yml`，后续新建项目时应直接在对应项目目录里写好 compose 文件，再进入该目录执行 `docker-compose up -d`。
- 主人还明确了升级约定：以后如果说要升级某个容器或项目，应进入对应项目目录，执行 `docker-compose pull && docker-compose up -d --remove-orphans`。
- 该约定已同步写入 `USER.md`、`MEMORY.md` 与 `TOOLS.md`，后续可直接按这个流程工作，不再反复确认。

## Heartbeat 精简摘要（20:14）
- 今天这条“影视源学习与配置维护线”的交互流程又细化了一步：当主人只给关键字时，我先在对应代号文件里搜索并列举候选项，再由主人点名具体替换对象。
- 这个新流程已经同时写进长期记忆、用户偏好和今日日记，后续即使跨会话也应保持，不再直接凭感觉猜测要改哪一条源。
- 当前这条线已经具备三层稳定支撑：代号映射、类型化学习笔记、关键字检索后确认的修改流程。

## Heartbeat 精简摘要（20:44）
- 晚上继续把这条影视配置线的操作方式跑通了一轮，已经实际按“关键字搜索 -> 列候选 -> 主人指定 -> 执行修改”的新规则完成了不夜自用里的盘搜候选列举。
- 随后又继续完成了两次不夜配置实改：先把 `不夜自用` 与 `不夜分享` 的 `bili_all_mix` 名称从“B站”改为“视频”，再把 `bilibili` 的“影视”条目插到它上方，并都完成了 JSON 校验。
- 说明这条工作线现在不只是有规则和笔记，连“搜索确认式修改”的实际流程也已经验证通了。
- 主人提供了 OpenWrt 登录信息并已实测连通：`192.168.50.2:22`，用户 `root`；我已成功 SSH 登录到 `ImmortalWrt 24.10.5`，后续可直接在其上检查 Web 管理界面与缺失组件。
- 已实际验证这台 OpenWrt 的 LuCI Web 后台可正常访问和登录，`uhttpd` 正在监听 `80` 端口，核心 LuCI / rpcd / uhttpd 组件齐全，当前不需要为“网页登录”额外补装基础组件。
- 已按主人要求把 OpenClaw Web 从仅本机访问改为局域网访问：当前地址为 `http://192.168.50.100:18789/`，Gateway 绑定已变为 `lan`，监听 `0.0.0.0:18789`。
- 随后已把 OpenClaw 登录方式从 token 改为 password，主人指定密码为 `Aa1992353`；服务重启后已恢复正常监听。
