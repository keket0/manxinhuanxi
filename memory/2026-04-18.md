# 2026-04-18

## 今日完成
- 记录主人新增的操作边界：以后凡是主人发来“需要修改的东西”，无论是不是 JS，都必须先等主人明确指令，再去操作，不能看到内容就自行修改、合并、落盘、发送或执行。
- 在主人明确下令后，处理了一份新的“影巢分组”JS：按要求填入 `hdhive_api_key` 与 `tmdb_api_key`，并把站点名称改为 `🎬 影巢| 分组`。
- 将修改后的 JS 导出到 `/www/manmanai/openclaw/exports/影巢分组_已填值.js`，便于后续发送或复用。
- 补充尝试过一条更简单的“接口源替换”口令思路，但主人当场否掉，决定后续仍由主人自己再想更顺手的说法，我这边先不固化新口令。
- 按主人要求完成一次 OpenClaw 快照备份，生成目录：`/www/manmanai/openclaw/backup/snapshots/2026-04-18-100705`，包含 `openclaw.json`、`workspace`、`agents` 与 `openclaw-gateway.service`。
- 按主人要求新增并学习了一批技能，重点核读了 `vetter`、`self-evolving`、`summarize-1-0-0`、`liang-tavily-search`、`find-skills-for-clawhub`、`agent-browser-clawdbot` 的说明。
- 为读取技能截图，先补装了 OCR 基础组件：`tesseract-ocr`、`tesseract-ocr-chi-sim` 与 Python 包 `pytesseract`。
- 已验证 `liang-tavily-search` 可实际运行：从 `/root/.openclaw/openclaw.json` 读取到已配置的 Tavily key，并成功返回 OpenClaw 相关搜索结果。
- 已修正 `summarize-1-0-0` 的依赖安装错误：先发现 `summarize-cli` 装错，不提供 `summarize` 命令，随后改装正确包 `@steipete/summarize`。
- 已验证 `summarize` 提取与摘要两种模式都能跑通：`--extract-only` 与 `--length short` 均可成功处理 `https://example.com`。
- 已验证 `agent-browser-clawdbot` 可用：命令 `agent-browser` 已存在，版本为 `0.25.4`，并成功打开 `https://example.com`、执行 `snapshot -i --json` 与关闭浏览器。
- 主人已明确：网页自动化默认主链路改为 `agent-browser`（skill：`agent-browser-clawdbot`），OpenClaw 内置 `browser` 工具降为备用链路。
- 已按主人要求下载一条 X 视频到 `/www/manmanai/openclaw/downloads/x/`，首次直连超时后改用单次代理成功下载，并已通过 Telegram 发送文件。
- 已核对该 X 视频源信息：原始宽高为 `1276x720`，属于横版源；主人反馈“视频比例不对”后，已确认不是下载链路拉伸导致，随后按要求重发原版文件。

## 关键决定
- “先等指令再操作”从只针对“全新的 JS”收紧为适用于所有“需要修改的东西”。
- 即使我已经识别出文件内容和处理方向，只要主人还没明确下令，也不能擅自动手。
- `不夜影视接口` 代号地址已更新为 `https://by.keket.cn:7777/tv1?token=1992353`。
- 但主人随后又明确补充：以后如果是去影视接口“找源”，默认源 token 仍按 `123456`，不要跟着代号地址改成 `1992353`。
- 进一步补充：如果真的要编辑到 `不夜分享` 或 `不夜自用`，写回文件时 token 也要改成 `123456`，不能继续沿用原有的 `1992353`。
- 关于“接口源替换”的简化口令，本次只讨论到备选思路，主人最终没有采纳；因此后续不把任何新口令视为长期规则，等主人正式定稿后再记。
- 主人已明确：网页自动化后续默认主链路改为 `agent-browser`（skill：`agent-browser-clawdbot`），OpenClaw 内置 `browser` 工具降为备用链路。
- 主人进一步收紧了 GitHub 备份边界：以后备份到 GitHub 私有仓库时，只备份龙虾的重要配置，不备份下载内容这类大体积运行产物。

## 遇到的失败/阻塞
- 本次 heartbeat 检查时发现 `memory/2026-04-18.md` 还不存在，已补建。
- 修改后的 `影巢分组_已填值.js` 落在大文件目录 `/www/manmanai/openclaw/exports/`，不在 workspace 仓库内，无法随 workspace 一起提交。
- 初次为 `summarize-1-0-0` 补依赖时装错了 npm 包：`summarize-cli` 只提供 `summarize-cli` 命令，不提供技能实际需要的 `summarize` 命令，后续已纠正为 `@steipete/summarize`。
- 下载 X 视频时首次直连抓取超时，后续改用单次代理成功，不需要动全局代理配置。

## 下一步
- 若主人要求，把 `影巢分组_已填值.js` 直接发回 Telegram。
- 若主人继续发新的待改文件，先只收下内容，等明确指令后再操作。
- 后续处理不夜专用 JS 时，按新固定流程执行：语法检查 → 先发下载版 JS → 询问是否推送到 `不夜影视组` → 同名文件先备份到 `/www/manmanai/buye/Cache` → 推送后重启并检查 `vod` 容器 → 成功后再删备份。
- 继续保持当天任务及时写入 daily note，避免跨日遗漏。
- 若主人后续还要处理 X/推特视频，默认优先复用 `yt-dlp + 单次代理` 这条链路。
- 后续若再做 GitHub 私有仓库备份，应改成“配置型备份”而不是整目录大包推送。

## 阶段小结
- 今天最重要的新规则不是某个 JS 细节，而是操作边界进一步收紧：以后看到待修改内容先等明确指令，这会直接影响后续所有文件类任务的处理节奏。
- 主人还补了一条不夜专用 JS 的发布规范，后续这条会成为固定流水线：先校验、先给下载版、再问是否推送、推送前备份、推送后重启并验活、成功再清备份；并且如果同名文件或 `key` 冲突，也要先备份到 `/www/manmanai/buye/Cache`。
- 今天还新增了一份 OpenClaw 整体快照，可作为后续回滚和迁移参考点。
- 新装技能里，当前已实跑验证通过的是 `liang-tavily-search`、`summarize-1-0-0` 与 `agent-browser-clawdbot`；其中 Tavily 已确认 key 可用，summarize 已确认命令与基础摘要链路可用，`agent-browser` 已被主人指定为网页自动化默认主链路。
